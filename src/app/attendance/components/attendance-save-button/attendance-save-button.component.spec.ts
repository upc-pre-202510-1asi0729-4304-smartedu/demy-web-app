/* import { ComponentFixture, TestBed } from '@angular/core/testing';

import { AttendanceSaveButtonComponent } from './attendance-save-button.component';

describe('AttendanceSaveButtonComponent', () => {
  let component: AttendanceSaveButtonComponent;
  let fixture: ComponentFixture<AttendanceSaveButtonComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [AttendanceSaveButtonComponent]
    })
    .compileComponents();

    fixture = TestBed.createComponent(AttendanceSaveButtonComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});  */
